"use client";
import { motion } from "framer-motion";
import Image from "next/image";

export default function TripCard({ trip }) {
  return (
    <motion.div 
      whileHover={{ y: -10 }}
      className="bg-white/5 border border-white/10 rounded-2xl overflow-hidden backdrop-blur-sm group cursor-pointer"
    >
      <div className="relative h-64 w-full overflow-hidden">
        <Image 
          src={trip.image} 
          alt={trip.title} 
          fill 
          className="object-cover transition-transform duration-500 group-hover:scale-110"
        />
        <div className="absolute top-4 right-4 bg-white/20 backdrop-blur-md px-3 py-1 rounded-full text-white text-sm">
          ⭐ {trip.ratings}
        </div>
      </div>
      
      <div className="p-6">
        <h3 className="text-xl font-bold text-white mb-2">{trip.title}</h3>
        <p className="text-gray-400 text-sm mb-4 flex items-center gap-1">
           📍 {trip.location}
        </p>
        <div className="flex justify-between items-center">
          <div>
            <span className="text-xs text-gray-500 block">Starts from</span>
            <span className="text-2xl font-black text-brand-sunset">₹{trip.price}</span>
          </div>
          <button className="bg-brand-ocean px-4 py-2 rounded-lg text-white font-medium hover:brightness-110">
            Book Now
          </button>
        </div>
      </div>
    </motion.div>
  );
}